# Normalize line endings: store and check out LF everywhere.
* text=auto eol=lf

# Binary assets — never normalize.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.wasm binary
*.woff  binary
*.woff2 binary

# SVG is text (XML).
*.svg text
